# Wave node-index parquets (~42 MB total) are stored in Git LFS. At runtime
# they are fetched from the GitHub LFS media URL from the main branch
data/h2o_wave_hindcast_index/** filter=lfs diff=lfs merge=lfs -text
